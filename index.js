const fn = () => {
    const miString = "soy un string!"
    let b = miString + "muy feliz"
    console.log(b)
    //control b para ocultar el menu bar
    //control p para la paleta de comandos
    //control k, sulto la k para poner la p (sin soltar control) para ver los archivos abiertos
    // control shift e, para cambiar entre archivos

}

fn()

const clickeado = () => {
    let a = "chanchito feliz"

    console.log(a)
}